.category
  %h2
    = link_to t('forem.forum.forums'), forem.forums_path
    » #{link_to forem_emojify(@category), [forem, @category]}
    = link_to "Create Topic", forem.new_forum_topic_path, [forem, @category]
  = render @category
